---
layout: base.njk
title: "Arbejdsproces — NORDI'SIGN"
description: "Fra intention til færdig film i 5 trin. Strategisk afklaring, visuel retning, verdensopbygning, komposition og levering."
extraHead: |
  <style>
    .page-hero {
      padding: 160px 40px 80px; text-align: center; background: var(--off-white);
    }
    .page-hero .section-label {
      font-family: var(--font-body); font-size: 11px; font-weight: 500;
      letter-spacing: 4px; text-transform: uppercase; color: var(--accent); margin-bottom: 16px;
    }
    .page-hero h1 {
      font-family: var(--font-serif); font-size: clamp(2.5rem, 5vw, 4rem);
      font-weight: 300; color: var(--text-dark); line-height: 1.2;
    }
    section { padding: 100px 40px; }
    .section-label {
      font-family: var(--font-body); font-size: 11px; font-weight: 500;
      letter-spacing: 4px; text-transform: uppercase; color: var(--accent); margin-bottom: 20px;
    }
    .process { background: var(--white); }
    .process-steps { display: grid; grid-template-columns: repeat(5, 1fr); gap: 24px; margin-top: 60px; }
    .step { padding: 32px 20px; border-top: 2px solid var(--mid-gray); transition: border-color 0.3s; }
    .step:hover { border-color: var(--accent); }
    .step-number { font-family: var(--font-display); font-size: 3.5rem; color: var(--accent); margin-bottom: 16px; font-weight: 700; line-height: 1; }
    .step h4 { font-family: var(--font-serif); font-size: 1.1rem; font-weight: 400; color: var(--text-dark); margin-bottom: 10px; }
    .step p { font-family: var(--font-serif); font-size: 14px; color: var(--text-muted); line-height: 1.7; }
    .parallax-break {
      height: 50vh; min-height: 350px;
      background-attachment: fixed; background-size: cover; background-position: center;
      position: relative; display: flex; align-items: center; justify-content: center;
    }
    .parallax-break::after { content: ''; position: absolute; inset: 0; background: rgba(0,0,0,0.35); }
    .parallax-quote { position: relative; z-index: 2; text-align: center; padding: 0 40px; max-width: 800px; }
    .parallax-quote blockquote { font-family: var(--font-serif); font-size: clamp(1.4rem, 3vw, 2.2rem); font-weight: 300; font-style: italic; color: #fff; line-height: 1.6; margin-bottom: 16px; }
    .parallax-quote cite { font-family: var(--font-body); font-size: 11px; letter-spacing: 3px; text-transform: uppercase; color: rgba(255,255,255,0.6); font-style: normal; }
    .parallax-2 { background-image: url('/assets/images/bg-gold-2.jpg'); }
    .btn-primary {
      display: inline-block; background: var(--text-dark); color: var(--white);
      padding: 14px 40px; font-family: var(--font-body); font-size: 11px; font-weight: 500;
      letter-spacing: 2.5px; text-transform: uppercase; text-decoration: none;
      transition: all 0.3s;
    }
    .btn-primary:hover { opacity: 0.85; transform: translateY(-2px); }
    .cta-inline { padding: 80px 40px; text-align: center; background: var(--off-white); }
    @media (max-width: 1024px) { .process-steps { grid-template-columns: repeat(3, 1fr); } }
    @media (max-width: 768px) {
      section { padding: 80px 24px; }
      .page-hero { padding: 120px 24px 60px; }
      .process-steps { grid-template-columns: 1fr 1fr; }
      .cta-inline { padding: 60px 24px; }
      .parallax-break { background-attachment: scroll; }
    }
    @media (max-width: 480px) { .process-steps { grid-template-columns: 1fr; } }
  </style>
---

<div class="page-hero">
  <p class="section-label">Arbejdsproces</p>
  <h1>Fra intention til f&aelig;rdig film</h1>
</div>

<section class="process">
  <div class="container">
    <div class="process-steps fade-in">
      <div class="step">
        <div class="step-number">01</div>
        <h4>Strategisk afklaring</h4>
        <p>Vi definerer den transformation, dit brand st&aring;r for.</p>
      </div>
      <div class="step">
        <div class="step-number">02</div>
        <h4>Visuel retning</h4>
        <p>Jeg udvikler et univers, der stemmer med jeres identitet og tone.</p>
      </div>
      <div class="step">
        <div class="step-number">03</div>
        <h4>Verdensopbygning</h4>
        <p>Billeder og sekvenser skabes og animeres med AI-v&aelig;rkt&oslash;jer.</p>
      </div>
      <div class="step">
        <div class="step-number">04</div>
        <h4>Komposition</h4>
        <p>Speak, rytme og visuel bev&aelig;gelse samles til en helhed.</p>
      </div>
      <div class="step">
        <div class="step-number">05</div>
        <h4>Levering</h4>
        <p>En f&aelig;rdig 1-minuts film klar til web og sociale medier.</p>
      </div>
    </div>
  </div>
</section>

<div class="parallax-break parallax-2">
  <div class="parallax-quote">
    <blockquote>&ldquo;Sanselighed er ikke pynt. Det er strategi.&rdquo;</blockquote>
    <cite class="script-text" style="font-size: 16px; letter-spacing: 1px;">Sophia Obel</cite>
  </div>
</div>

<div class="cta-inline fade-in">
  <a href="/kontakt/" class="btn-primary">Book en samtale</a>
</div>
